# CropDetec Frontend

This is the Flutter mobile frontend for the farmer-facing application.

The first phase contains a white, green, and black dashboard shell. The next frontend steps are:

- [x] Add environment configuration for the Laravel API URL.
- [x] Add environment configuration for the Laravel storage URL.
- [x] Build login and registration screens.
- [x] Add starter token/session storage abstraction.
- [x] Build image capture/gallery selection.
- [x] Connect diagnosis upload to `POST /api/diagnoses`.
- [x] Build diagnosis results and history screens.
- [ ] Add English/Shona resources and text-to-speech.
